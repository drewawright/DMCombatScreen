@model IEnumerable<DMCombatScreen.Models.ConditionListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConditionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConditionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConditionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConditionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ConditionID }) |
            @Html.ActionLink("Details", "Detail", new { id = item.ConditionID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ConditionID })
        </td>
    </tr>
}

</table>
